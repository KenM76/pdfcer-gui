# HANDOFF — 2026-09-09, early morning to mid-morning

> Written at the end of the session that closed the central-panel wobble,
> consumed the engine's overnight replies, released `v0.5.0-dev.20260909.1`,
> and then — with the operator's go-ahead — drove the full `ui-verify` suite
> against the released binary. `RESUME.md`'s top block is the short form; this
> is the long one, with the reasoning and the sweep's verdicts.

## 0. State, re-measured in this session

| | | measured with |
|---|---|---|
| shell | ``f7b512c` (+ this handoff commit)` on `origin/main`, tree clean | `git log -1`, `git status` |
| engine | `pdfcer-core` 0.49.0 at **`5b8ec61`** (`Pass 279.0`); the engine tree was at `15e997d` (`Pass 280.0`, the repertoire verb we asked for) at packaging time — `cargo update` first next build | `Cargo.lock`; `package-portable.py`'s NOTE |
| tests | **3,912 passing, 0 failing, 50 ignored** | `CARGO_BUILD_JOBS=2 cargo test --workspace`, every `test result` summed through `awk` |
| gates | **32 of 32, 0 skipped** | `bash tools/gates/run-all.sh` |
| driven checks registered | **196** | `Box::new(` inside `checks/roster.rs`'s `all()` |
| release | **Three today.** 06:25 `.1` → gui2; 08:36 `.2` → gui1; **10:22 `v0.5.0-dev.20260909.3` → `pdfcer-gui2` = the newest** (build `3ebeeec`), so `pdfcer-gui1` (08:27) is the fallback. All three tags verified at their built commits. ★ He runs `OneDrive\pdfcer\pdfcer-gui.exe`, which was the 08:27 build at 10:20 — so his reports were against a current build | `releases/latest`, tag sha via API |
| driven sweep | **stopped at 64 of 196 when the operator returned** — 63 PASS, 1 FAIL, 0 SKIP among those; see §4 | `target/scratch/sweep-full.log` |

## 1. The wobble — what it was, how it was found, what changed

**Symptom, two days old:** the central panel's width oscillated 0.1–0.5 pt on
isolated frames with nothing on screen changing. Five instruments across two
sessions each narrowed it by one container and stopped at "inside egui's
`Panel::show`".

**The instrument that found it** (`egui-shell/src/dock/overflow_probe.rs`): on
any frame where a side's frame rect crosses the window edge, publish every
widget egui registered this pass whose rect also crosses it, by egui id
(`ctx.viewport(|v| v.this_pass.widgets.layers())`). First read, off-screen,
same fixture and layout as the wobbling traces: five widgets, four containers,
one leaf — `[1390.0, 292.7]–[1400.4, 866.0]`, a 10-pt strip on the Comments
pane's right edge, decaying `1400.4 → 1400.1 → 1400.0`. Ten points at the
edge is the scroll bar (`bar_width = 10`); a decay is an animation.

**The mechanism, each part from egui 0.35 source:**

1. `Panel::show` takes its rect back from the frame's content union and, when
   it is wider than `exact_size`, restores the width by moving the INNER edge
   (`min.x = max.x − size`). `exact_size` fixes a width, not a position.
2. `scope_builder` ends by allocating the child's `min_rect()` into its
   parent, so a body's overflow climbed into the side's frame. `set_clip_rect`
   truncates paint, not layout — the comment beside it ("clipped, not
   accommodated") was true of pixels and false of rects.
3. A SOLID `ScrollStyle` has fractional bar use while its bar fades in
   (`show_bars_factor × allocated_width`, cubic-out). The content rect is
   rounded up to whole pixels, `auto_shrink` adopts the rounded width, the
   fraction is added back: `rounded(outer − f·W) + f·W` exceeds the pane by the
   rounding residue on mid-fade frames. Floating bars never do it (boolean
   bar use).

**Reproduced** in `egui-shell/src/dock/scroll_fade_repro.rs` — a right
`Panel::exact_size(320)` with a solid 10-pt `ScrollArea` whose content grows on
frame 5, fixed 60 fps clock: `+0.25` (frame 8), `+0.47` (frame 9), zero
elsewhere. It asserts the overflow so an egui fix turns it red.

**Fixed** in `egui-shell/src/dock/stack.rs` (moved out of `mod.rs`, which the
explanation pushed past R2): bodies draw in `ui.new_child` — never merged —
and the compartment's own rect is allocated in their place. The regression
guard (`overflow_probe.rs` tests) was written against the UNFIXED code first:
`[1080.4 .. 1400.4]`, the trace's line; green after.

**Kept:** `RESIZE_FLOOR_PT` in `fit.rs` (re-documented: source known and gone;
the floor stays because a sub-pixel viewport change is not a resize anyone
made). `overflow_probe` stays as the tripwire. `dock.<side>.body_min` is now
always equal to `.frame` and can go next release.

**RAG:** `D:/dev/rag/egui/a_solid_scrollbar_fading_in_overshoots_its_pane_and_an_exact_size_panel_slides_inward_by_the_excess.md`.
**Memory:** `feedback_when_a_wobble_is_inside_the_framework_list_the_leaves_by_id`.

## 2. The engine's overnight replies, and what each changed here

Both replies answer requests this shell filed. Read them in
`D:\Dev\FeatureRequests\pdfce_FeatureRequests\open\reply_2026-09-09-*`.

| engine | this shell |
|---|---|
| `Pass 275.0` — Enter in a Markup text box makes a new line (`encode_winansi` ran before `wrap_lines`; both branches were affected, not the one we reported) | consumed by the pin. No shell workaround ever existed. **Driven confirmation: §4.** |
| `Pass 274.0` — a font-coverage refusal names the faces that would work | **`wanted`** (`ENGINE_BACKLOG.md`): `textstyle::refusal_of` maps `CoverageFailure(_)` to a static sentence and drops `Refusal.message`. What it takes is in the row. |
| `Pass 273.0` — `/RC` dropped rather than left stale, disclosed | already consumed (`annots::set_markup_note` reads `rich_text_dropped`); row written as the report back. |
| `Pass 277.0` — `EditError::ResizeFixedSizeMarker` for a `/Text` or `NoZoom` annotation, AND a correction: the sticky's icon anchors at the rect's **UPPER-LEFT**, not lower-left as the engine's own doc said | `Declined::ResizeFixedSizeMarker { by_flag }` worded (reachable via the Properties geometry fields). **`canvas::clicking` now hangs the sticky's 20-pt square DOWN from the click** — before, the icon sat 20 pt above the click in Acrobat. Four citations of the wrong corner corrected. |

One request filed — and corrected before sending by grepping the engine tree
first: `preview_font_resources_for` already ships the pre-keystroke half, so
`request_which_characters_can_this_run_accept_before_the_first_keystroke.md`
says we wire that first and asks only about per-keystroke cost.

## 3. Harness and environment findings of the session

- **Launching from `target/scratch/drive/` with a RELATIVE fixture path opens
  no document.** The tell is a trace with no `status page=` line. Launch from
  the repo root.
- **`$!` after `(cmd & echo $!)` is the subshell's pid.** Two instances leaked
  before I noticed. Find by `Get-Process pdfcer-gui`, verify the path, kill
  by PID.
- **The `SetPropW` launch-killer is gone with the reboot** — five clean
  launches, zero deaths.
- **RAM:** `cargo test --workspace` at default jobs linked eight test binaries
  at ~1.3 GB each and the harness killed the shell for low memory — twice —
  leaving cargo/rustc/link as orphans that kept starving the machine. Kill
  them, then run `CARGO_BUILD_JOBS=2`, one cargo job at a time, never
  gates → tests → build chained in one background command.
- **`gh release create --target main` resolves on the SERVER.** With unpushed
  commits the tag landed on the previous session's last commit while the zip
  was built from the newest; `releases/latest` looked perfect. Push first.
  Fixed with `git tag -f` + forced tag push; the tag is at `4d9faaf`.
- **Two sweeps at once.** Killing one `ui-verify` while its driving shell loop
  survived let the loop start the next check, and a second detached launch
  then ran beside it — two harnesses on one pointer. Kill the LOOP (by command
  line), then the harness, then the GUI, and confirm `ui-verify: 1` before
  trusting a single verdict.

## 4. The driven sweep — verdicts

The operator granted the desktop at ~07:05 and took it back at ~07:50 with
the four-report message (§4b). The full suite was run in chunks of 20 against
the 06:21 release binary, `a1-titleblock.pdf`, `--doc-point 0,2000,320`,
`--second-pdf four-pages.pdf`, then killed cleanly (loop → harness → GUI,
`ui-verify: 0` confirmed). **Verdicts that survive: 64 checks — 63 PASS,
1 FAIL.** The other 132 never ran. Verdicts printed are complete per check,
so these 64 are not contaminated by the stop.

| | |
|---|---|
| PASS (63) | includes the three named in `RESUME.md` as needing it: `rotating_a_markup_turns_it`, `dragging_a_markup_moves_it`, `delete_key_after_canvas_click`, plus `a_foreign_icon_name_reaches_the_panel`, `the_typed_angle_turns_a_mark`, `a_text_file_becomes_pages`, `read_mode_copies_a_picture…`, `the_ladder_goes_as_deep_inside_a_container…` — full list: `grep PASS target/scratch/sweep-full.log` |
| **FAIL (1)** | **`double_clicking_a_text_box_edits_the_text`** — NOT investigated; the report is in the log under its name. Could be the check (doc-point aim on this fixture) or the feature; the memory rule is *a driven failure is a claim about the check too* — read what it aimed at first. |
| never ran (132) | everything from chunk 4's remainder on, including `resize_scales_a_shape` at its page-border aim, the `text_annot` family (Enter key), `comment_note`, all measure checks, forms, redaction, clipboard |

⚠ Two procedural defects of mine, recorded in §3: the first chunked run was
started under the tool's 10-minute limit and had to be restarted detached,
and for ~40 s two sweeps ran on one pointer before the first was killed —
those overlapped verdicts were discarded with the restart (the log was
deleted and the run began again from chunk 1).

## 4b. The four-report message, measured and answered

See `OPERATOR_REQUESTS.md` **O159** for the operator-facing account and the
commit message of `f7b512c` for the engineering one. In one line each:

1. **Stamp resize** — engine gap (third authoring family); request filed; our
   sentence was FALSE and is now *"can't resize a stamp's picture yet…"*.
2. **SW41177 text** — the remedy path already existed; the engine's
   first-named face was a no-op until `5b8ec61`; probe on his line: refused →
   Helvetica → ACCEPTED. Pin bump is the whole fix on our side.
3. **Redaction box** — ONE sheet (Excel 365 export) is hybrid-reference; the
   writer refuses a full rewrite by name; request filed; our sentence now
   names the form and the workaround. PDF RAG lesson written.
4. **Freehand nodes** — engine shipped `Pass 278.0` overnight; wired by a
   subagent this session (see `f7b512c`); NOT driven.

★ The engine is now shipping within the hour of a request (four times today).
**Read the channel before diagnosing any report** — three of the four were
already answered on the engine side when he wrote.

## 4c. The second report, 09:40 — three more, each measured on `SW41177-obselete.pdf`

`OPERATOR_REQUESTS.md` **O160**; commit `3ebeeec`'s message is the engineering account.

| report | cause, measured | change |
|---|---|---|
| *"found 35 piece(s) of the supposedly-removed text still in it"* | Ghostscript 8.15 draws one glyph per show operator → `redacted_text = ["3",".","5"," ","T","Y","P"]`; the proof's REFUSAL half had no length floor and found `"3"` on all 24 pages | floor governs both halves; short pieces counted + disclosed; probe on his file now `glyphs=7 short=7 residuals=0 clean=true`. Engine asked for word-level `redacted_text`. PDF RAG lesson. |
| *"STILL can't adjust the size of a stamp"* | engine carries a foreign `/AP` only with `scale_stroke_width` (uniform) / `allow_appearance_distortion`; our default modifiers set neither, so a stamp (third authoring family) was refused | `annots::resize` sets both for a `/Stamp`; canvas grips and Properties fields share the function. `ResizeStampNotYet` deleted. Two unit tests, red before. |
| *"the text comes out vertical … no control to set the angle"* | every page `/Rotate 90`; authoring in user space is correct and displays sideways | `textannot::commit` composes `set_annotation_rotation(+rotate)` about the rect centre in the same undo step (sticky excluded, `NoRotate`); the Angle field now appears because the mark has an angle. `fixtures/rotated-90.pdf` + generator; unit test. |

★ His follow-up — *"if it is rejecting based on a search of finding other items with the same text elsewhere then that is a bug and not a feature"* — was the diagnosis, stated before I had finished measuring it. The probe is `redact::tests::where_do_the_survivors_live_on_his_drawing` (ignored); `sealed.rs` correctly refused its first draft for calling the engine's removal directly.

## 5. What to do next

1. **Drive, in this order, when the desktop is free:** (0) the three O160 fixes — a stamp resize from a grip and from the Properties width field, a stamp placed on `rotated-90.pdf` reading upright (pixel oracle), a marquee redaction on a Ghostscript per-glyph file preparing and applying; (a) the 132 checks
   that never ran — `target/scratch/checks.txt` lines 65–196 with the
   command in `target/scratch/sweep-full.sh` (run it DETACHED via
   `Start-Process cmd /c bash …`, never inside the tool's 10-minute window,
   and confirm `ui-verify: 1` before trusting anything); (b) investigate
   `double_clicking_a_text_box_edits_the_text` — read its aim before its
   verdict; (c) write the ink-node driven check the subagent specified (in
   `f7b512c`'s message and `EDITABLE_SURFACES.md`'s `reshape_ink` row).
1b. **Wire `run_repertoire`** (engine `Pass 280.0`, `15e997d` — NOT in the
   pin yet): `cargo update`, then the in-place editor refuses a keystroke
   outside the run's accepted set at the key. `ENGINE_BACKLOG.md` has the
   design paragraph.
1c. **Two engine requests are open** (stamp resize; hybrid full rewrite).
   When either ships: delete `Declined::ResizeStampNotYet` /
   `text::status::resize::resize_stamp_not_yet`, or the hybrid arm in
   `text::redact::refusal_message` — both are dated and name their request.
2. `ENGINE_BACKLOG.md`'s `wanted` row for the font-coverage remedy — a
   `String`-carrying `Declined` (first non-static sentence; `line()` grows a
   `Cow`), then the engine's face list appended after our sentence.
3. Wire `preview_font_resources_for` per keystroke in the in-place editor
   (refuse at the key, keep the word) — the request file says we will.
4. `canvas.selection-outline` published twice per frame — unchanged,
   `HANDOFF_20260908_RESIZE.md` §3.4.
5. Delete `dock.<side>.body_min` (three lines in `draw_side`).
6. `cargo update` the engine before the next build — it moved again.

## 6. What NOT to do

- Do not `taskkill /IM pdfcer-gui.exe` — he uses it all day. PID + path.
- Do not weaken `resize_scales_a_shape`'s aim (page-sized selection is the
  scrollbar regression test).
- Do not delete `overflow_probe` or `RESIZE_FLOOR_PT`; both docs say why they stay.
- Do not run the suite at default cargo jobs on this machine.
- Do not `gh release create` before `git push`.

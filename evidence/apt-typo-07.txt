ui-verify — profile `pdfcer-gui` (the application this project is building (crates/pdfcer-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[SKIP] a_refused_typo_fix_says_why_it_was_refused
       detects: text the document arrived with is committed for edit, the engine refuses it, and the operator is told nothing — while text pdfcer itself added edits fine, so from his chair the program silently ignores corrections to exactly the words he wants to correct
       · fixture C:/Users/Ken/AppData/Local/Temp/claude/D--Dev-pdfcer-gui/b87679c0-5e3b-4a4a-b478-a76640dddb66/scratchpad/apt.pdf — page 1 is 612 x 791.9999937 pt; aiming at (200.0, 537.0) on page 2 (1-based)
       · launched target/release/pdfcer-gui.exe as pid 46548 with PDFCER_DIAG_INVOKE=view.reset_layout,mode.edit,edit.text
       · ★ the dock layout was reset, and the application said so: `pdfcer-diag layout-reset scope=all changed=false`
       · ★ rolled the wheel 3 step(s) to bring page 2 onto the canvas
       · ★ the click placed a caret: `pdfcer-diag text-edit-caret kind=Edit page=1 run=12 len=36`
       · ★★ the engine refused the commit: `pdfcer-diag edit-text-refused page=1 n=1 detail=text to edit ("Final quality walkthrough with clien") was not found in an editable run on the page`
       · ★★ and it named the CATEGORY: the engine's kind=NotFound, the sentence chosen=SplitAcrossPieces — so what the operator read was chosen rather than defaulted
       · ★★★ and the category was RESOLVED rather than forwarded: the engine's `NotFound` plus the shell's own `edit-text-pin` measurement produced `SplitAcrossPieces` — the sentence that names the cause instead of the symptom
       · ★★★ the refusal was DISCLOSED — the `⊗` slot drew on a frame after it
       → the negative control could not find bare paper. It clicked x=200 at y
         offsets -55, -110, +55, +110 from the aim, and every one either opened no
         draft or landed on a RUN. ★ The hit test snaps to the nearest run on the
         LINE, so a horizontal step does not leave a line of text however far it goes
         — an earlier version stepped 250 pt and then to the right margin and
         resolved the SAME run both times. What leaves a line is a vertical offset,
         and this page has none free within 110 pt of the aim. Without a successful
         edit through the same instrument this check has measured a region with no
         baseline, and its positive arm is not a verdict. SKIPPED rather than failed.
         Trace: target/ui-verify\typo-refusal.trace.txt.
       artifact: target/ui-verify\typo-refusal.trace.txt

------------------------------------------------------------------------
  0 passed, 0 failed, 1 skipped

RESULT: NOTHING VERIFIED — no check reached a verdict.

  This is NOT a pass. Every check above was unable to begin, and the
  reasons are printed with each one. A suite that cannot run has told
  you nothing about the application, and 'told you nothing' rendered
  as green is the exact failure this harness exists to remove.
